以下是完整的 CSS `@media screen` 屏幕适配方案，包含多种常见的适配场景：

## 🎯 基础断点设置

```css
/* 移动设备优先的响应式设计 */
/* 默认样式（移动设备） */
.container {
  padding: 16px;
  font-size: 14px;
}

/* 平板设备 (768px 及以上) */
@media screen and (min-width: 768px) {
  .container {
    padding: 24px;
    font-size: 16px;
  }
}

/* 桌面设备 (1024px 及以上) */
@media screen and (min-width: 1024px) {
  .container {
    padding: 32px;
    font-size: 18px;
    max-width: 1200px;
    margin: 0 auto;
  }
}

/* 大屏设备 (1440px 及以上) */
@media screen and (min-width: 1440px) {
  .container {
    padding: 40px;
    font-size: 20px;
    max-width: 1400px;
  }
}
```

## 📱 完整的响应式布局系统

### 1. 栅格系统适配

```css
/* 响应式栅格系统 */
.grid {
  display: grid;
  gap: 16px;
  padding: 16px;
}

/* 移动端：1列 */
@media screen and (max-width: 767px) {
  .grid {
    grid-template-columns: 1fr;
  }
  
  .card {
    margin-bottom: 16px;
  }
}

/* 平板端：2列 */
@media screen and (min-width: 768px) and (max-width: 1023px) {
  .grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }
}

/* 桌面端：3列 */
@media screen and (min-width: 1024px) and (max-width: 1439px) {
  .grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }
}

/* 大屏：4列 */
@media screen and (min-width: 1440px) {
  .grid {
    grid-template-columns: repeat(4, 1fr);
    gap: 32px;
  }
}
```

### 2. 导航栏适配

```css
/* 移动端导航 */
.nav {
  display: flex;
  flex-direction: column;
  background: #333;
  padding: 16px;
}

.nav-item {
  padding: 12px;
  color: white;
  text-decoration: none;
  border-bottom: 1px solid #555;
}

.menu-toggle {
  display: block;
  background: none;
  border: none;
  color: white;
  font-size: 24px;
  cursor: pointer;
}

/* 桌面端导航 */
@media screen and (min-width: 768px) {
  .nav {
    flex-direction: row;
    justify-content: space-between;
    align-items: center;
  }
  
  .nav-item {
    border-bottom: none;
    margin: 0 16px;
  }
  
  .menu-toggle {
    display: none;
  }
  
  .nav-links {
    display: flex !important; /* 覆盖移动端的隐藏 */
  }
}

/* 移动端菜单隐藏 */
@media screen and (max-width: 767px) {
  .nav-links {
    display: none;
  }
  
  .nav-links.active {
    display: flex;
    flex-direction: column;
  }
}
```

### 3. 字体大小适配

```css
/* 响应式字体系统 */
:root {
  /* 基础字体大小 */
  --font-size-sm: 12px;
  --font-size-base: 14px;
  --font-size-lg: 16px;
  --font-size-xl: 18px;
  --font-size-2xl: 20px;
}

body {
  font-size: var(--font-size-base);
  line-height: 1.5;
}

h1 {
  font-size: 24px;
  line-height: 1.2;
}

h2 {
  font-size: 20px;
  line-height: 1.3;
}

/* 平板端字体调整 */
@media screen and (min-width: 768px) {
  body {
    font-size: var(--font-size-lg);
  }
  
  h1 {
    font-size: 32px;
  }
  
  h2 {
    font-size: 24px;
  }
}

/* 桌面端字体调整 */
@media screen and (min-width: 1024px) {
  body {
    font-size: var(--font-size-xl);
  }
  
  h1 {
    font-size: 40px;
  }
  
  h2 {
    font-size: 28px;
  }
}

/* 大屏字体调整 */
@media screen and (min-width: 1440px) {
  body {
    font-size: var(--font-size-2xl);
  }
  
  h1 {
    font-size: 48px;
  }
  
  h2 {
    font-size: 32px;
  }
}
```

## 🖥️ 特殊场景适配

### 4. 横屏适配

```css
/* 横屏模式适配 */
@media screen and (orientation: landscape) and (max-height: 500px) {
  .header {
    height: 60px;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
  }
  
  .content {
    margin-top: 60px;
    height: calc(100vh - 60px);
    overflow-y: auto;
  }
  
  .mobile-nav {
    display: none; /* 横屏时隐藏移动导航 */
  }
}
```

### 5. 高分辨率屏幕适配

```css
/* Retina 屏幕适配 */
@media screen and (-webkit-min-device-pixel-ratio: 2),
       screen and (min-resolution: 192dpi) {
  .logo {
    background-image: url('logo@2x.png');
    background-size: contain;
  }
  
  .icon {
    image-rendering: -webkit-optimize-contrast;
    image-rendering: crisp-edges;
  }
}

/* 4K 屏幕适配 */
@media screen and (min-width: 2000px) {
  .container {
    max-width: 1800px;
    font-size: 22px;
  }
  
  .grid {
    gap: 48px;
  }
}
```

### 6. 暗色模式适配

```css
/* 暗色模式 */
@media (prefers-color-scheme: dark) {
  :root {
    --bg-color: #1a1a1a;
    --text-color: #ffffff;
    --border-color: #333333;
  }
  
  body {
    background-color: var(--bg-color);
    color: var(--text-color);
  }
  
  .card {
    background: #2d2d2d;
    border-color: var(--border-color);
  }
}

/* 亮色模式 */
@media (prefers-color-scheme: light) {
  :root {
    --bg-color: #ffffff;
    --text-color: #333333;
    --border-color: #dddddd;
  }
}
```

## 🔧 实用工具类

### 7. 显示/隐藏适配

```css
/* 响应式显示工具 */
.mobile-only {
  display: block;
}

.tablet-only {
  display: none;
}

.desktop-only {
  display: none;
}

/* 平板端显示规则 */
@media screen and (min-width: 768px) and (max-width: 1023px) {
  .mobile-only {
    display: none;
  }
  
  .tablet-only {
    display: block;
  }
}

/* 桌面端显示规则 */
@media screen and (min-width: 1024px) {
  .mobile-only {
    display: none;
  }
  
  .tablet-only {
    display: none;
  }
  
  .desktop-only {
    display: block;
  }
}

/* 打印样式 */
@media print {
  .no-print {
    display: none !important;
  }
  
  body {
    font-size: 12pt;
    line-height: 1.4;
  }
}
```

### 8. 间距适配

```css
/* 响应式间距系统 */
:root {
  --space-xs: 4px;
  --space-sm: 8px;
  --space-md: 16px;
  --space-lg: 24px;
  --space-xl: 32px;
}

.section {
  padding: var(--space-md);
  margin-bottom: var(--space-md);
}

/* 平板端间距 */
@media screen and (min-width: 768px) {
  .section {
    padding: var(--space-lg);
    margin-bottom: var(--space-lg);
  }
}

/* 桌面端间距 */
@media screen and (min-width: 1024px) {
  .section {
    padding: var(--space-xl);
    margin-bottom: var(--space-xl);
  }
}
```

## 🚀 完整示例页面

```css
/* 完整的响应式页面示例 */
.page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.header {
  background: #2c3e50;
  color: white;
  padding: 16px;
  position: sticky;
  top: 0;
  z-index: 100;
}

.main {
  flex: 1;
  padding: 16px;
}

.footer {
  background: #34495e;
  color: white;
  padding: 16px;
  text-align: center;
}

/* 移动端布局 */
@media screen and (max-width: 767px) {
  .header nav {
    display: none; /* 移动端隐藏导航 */
  }
  
  .mobile-menu-btn {
    display: block;
  }
  
  .content-grid {
    grid-template-columns: 1fr;
  }
}

/* 平板端布局 */
@media screen and (min-width: 768px) and (max-width: 1023px) {
  .header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .mobile-menu-btn {
    display: none;
  }
  
  .content-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

/* 桌面端布局 */
@media screen and (min-width: 1024px) {
  .header {
    padding: 24px 5%;
  }
  
  .main {
    padding: 32px 5%;
  }
  
  .footer {
    padding: 24px 5%;
  }
  
  .content-grid {
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }
}

/* 超大屏布局 */
@media screen and (min-width: 1440px) {
  .header,
  .main,
  .footer {
    padding-left: 10%;
    padding-right: 10%;
  }
  
  .content-grid {
    grid-template-columns: repeat(4, 1fr);
    gap: 40px;
  }
}
```

## 💡 最佳实践建议

### 断点选择策略：
```css
/* 推荐断点（基于主流设备） */
/* 手机: < 768px (默认样式) */
/* 平板: 768px - 1023px */
/* 桌面: 1024px - 1439px */
/* 大屏: >= 1440px */
```

### 性能优化：
```css
/* 使用 CSS 变量管理响应式值 */
:root {
  --breakpoint-mobile: 768px;
  --breakpoint-tablet: 1024px;
  --breakpoint-desktop: 1440px;
}

@media screen and (min-width: var(--breakpoint-tablet)) {
  /* 平板和桌面样式 */
}
```

### 移动优先原则：
```css
/* ✅ 推荐：移动优先 */
.element {
  /* 移动端样式 */
}

@media screen and (min-width: 768px) {
  .element {
    /* 平板端增强 */
  }
}

/* ❌ 避免：桌面优先 */
@media screen and (max-width: 767px) {
  .element {
    /* 移动端覆盖 */
  }
}

@media screen and (min-width:620px){
   .summaryBlock{
    grid-template-columns: repeat(2,1fr);
   }
}
@media screen and (min-width:920px){
    .summaryBlock{
        grid-template-columns: repeat(3,1fr);
       }
}
@media screen and (min-width:1300px){
    .summaryBlock{
        grid-template-columns: repeat(4,1fr);
    }
}
@media screen and (min-width:1600){
    .summaryBlock{
        grid-template-columns: repeat(5,1fr);
       }
}
```

这个完整的媒体查询适配方案涵盖了大多数常见的响应式需求，你可以根据具体项目选择合适的部分使用。记住保持断点的一致性，并采用移动优先的开发策略。


针对容器尺寸的适配可以使用 **CSS Container Queries**，这是现代 CSS 的强大功能。以下是完整的解决方案：

## 🎯 CSS 容器查询基础

### 1. 基础容器查询语法

```css
/* 定义容器 */
.component-container {
  container-type: inline-size;
  container-name: component;
}

/* 基于容器宽度的适配 */
@container component (min-width: 400px) {
  .card {
    display: flex;
    gap: 20px;
  }
  
  .card-image {
    width: 120px;
    height: 120px;
  }
}

@container component (min-width: 600px) {
  .card {
    padding: 24px;
  }
  
  .card-title {
    font-size: 24px;
  }
}
```

### 2. 完整的组件适配示例

```css
/* 卡片组件容器 */
.card-container {
  container-type: inline-size;
  container-name: card;
  margin: 16px;
}

/* 卡片基础样式（小容器） */
.card {
  background: white;
  border-radius: 8px;
  box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  padding: 16px;
  transition: all 0.3s ease;
}

.card-image {
  width: 100%;
  height: 160px;
  object-fit: cover;
  border-radius: 4px;
  margin-bottom: 12px;
}

.card-title {
  font-size: 18px;
  font-weight: bold;
  margin-bottom: 8px;
}

.card-description {
  color: #666;
  font-size: 14px;
  line-height: 1.4;
}

/* 中等宽度容器适配 */
@container card (min-width: 400px) {
  .card {
    display: flex;
    gap: 16px;
    align-items: flex-start;
  }
  
  .card-image {
    width: 120px;
    height: 120px;
    margin-bottom: 0;
    flex-shrink: 0;
  }
  
  .card-content {
    flex: 1;
  }
}

/* 大宽度容器适配 */
@container card (min-width: 600px) {
  .card {
    padding: 24px;
    gap: 24px;
  }
  
  .card-image {
    width: 160px;
    height: 160px;
  }
  
  .card-title {
    font-size: 24px;
  }
  
  .card-description {
    font-size: 16px;
    line-height: 1.6;
  }
}

/* 超大容器适配 */
@container card (min-width: 800px) {
  .card {
    display: block;
    text-align: center;
    padding: 32px;
  }
  
  .card-image {
    width: 200px;
    height: 200px;
    margin: 0 auto 20px;
    border-radius: 50%;
  }
  
  .card-title {
    font-size: 28px;
    margin-bottom: 16px;
  }
}
```

## 🛠️ 实际应用场景

### 3. 栅格布局中的容器查询

```css
/* 网格布局 */
.responsive-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
  gap: 24px;
  padding: 20px;
}

.grid-item {
  container-type: inline-size;
  container-name: grid-item;
}

/* 根据网格项宽度自适应 */
@container grid-item (min-width: 300px) {
  .product-card {
    padding: 16px;
  }
  
  .product-image {
    height: 200px;
  }
}

@container grid-item (min-width: 400px) {
  .product-card {
    display: flex;
    padding: 20px;
  }
  
  .product-image {
    width: 150px;
    height: 150px;
    margin-right: 16px;
  }
}

@container grid-item (min-width: 500px) {
  .product-card {
    flex-direction: column;
    text-align: center;
    padding: 24px;
  }
  
  .product-image {
    width: 100%;
    height: 220px;
    margin-right: 0;
    margin-bottom: 16px;
  }
}
```

### 4. 侧边栏组件适配

```css
.sidebar {
  container-type: inline-size;
  container-name: sidebar;
  width: 100%;
  background: #f5f5f5;
  padding: 16px;
}

/* 窄侧边栏 */
@container sidebar (max-width: 200px) {
  .sidebar-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 8px 4px;
  }
  
  .sidebar-icon {
    font-size: 20px;
    margin-bottom: 4px;
  }
  
  .sidebar-text {
    font-size: 12px;
  }
}

/* 中等侧边栏 */
@container sidebar (min-width: 200px) and (max-width: 300px) {
  .sidebar-item {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 12px 8px;
  }
  
  .sidebar-icon {
    font-size: 18px;
    width: 24px;
  }
  
  .sidebar-text {
    font-size: 14px;
  }
}

/* 宽侧边栏 */
@container sidebar (min-width: 300px) {
  .sidebar-item {
    display: flex;
    align-items: center;
    gap: 16px;
    padding: 16px 12px;
    border-radius: 8px;
    transition: background-color 0.2s;
  }
  
  .sidebar-item:hover {
    background: #e0e0e0;
  }
  
  .sidebar-icon {
    font-size: 20px;
    width: 24px;
  }
  
  .sidebar-text {
    font-size: 16px;
    font-weight: 500;
  }
  
  .sidebar-badge {
    margin-left: auto;
    background: #007bff;
    color: white;
    padding: 2px 8px;
    border-radius: 12px;
    font-size: 12px;
  }
}
```

## 📊 容器查询类型

### 5. 不同类型的容器查询

```css
.component {
  container-type: inline-size; /* 只查询宽度 */
  container-name: my-component;
}

.layout-container {
  container-type: size; /* 查询宽度和高度 */
  container-name: layout;
}

.style-container {
  container-type: style; /* 查询CSS自定义属性 */
  container-name: styles;
}

/* 宽度查询 */
@container my-component (min-width: 300px) {
  .content { display: flex; }
}

/* 高度查询 */
@container layout (min-height: 400px) {
  .section { padding: 40px; }
}

/* 宽高组合查询 */
@container layout (min-width: 500px) and (min-height: 300px) {
  .hero { font-size: 2rem; }
}
```

## 🔧 高级用法

### 6. 嵌套容器查询

```css
.dashboard {
  container-type: inline-size;
  container-name: dashboard;
}

.widget {
  container-type: inline-size;
  container-name: widget;
  margin: 16px;
}

/* 外层容器查询 */
@container dashboard (min-width: 800px) {
  .widgets-grid {
    grid-template-columns: repeat(2, 1fr);
  }
}

@container dashboard (min-width: 1200px) {
  .widgets-grid {
    grid-template-columns: repeat(3, 1fr);
  }
}

/* 内层容器查询 */
@container widget (min-width: 300px) {
  .metric-card {
    padding: 20px;
  }
  
  .metric-value {
    font-size: 24px;
  }
}

@container widget (min-width: 400px) {
  .metric-card {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  
  .metric-value {
    font-size: 32px;
  }
}
```

### 7. 容器查询与 CSS 变量结合

```css
.themeable-component {
  container-type: inline-size;
  container-name: themeable;
  --card-padding: 16px;
  --image-size: 80px;
}

@container themeable (min-width: 400px) {
  .themeable-component {
    --card-padding: 24px;
    --image-size: 120px;
  }
}

@container themeable (min-width: 600px) {
  .themeable-component {
    --card-padding: 32px;
    --image-size: 160px;
  }
}

.card {
  padding: var(--card-padding);
}

.avatar {
  width: var(--image-size);
  height: var(--image-size);
}
```

## 🚀 实际 HTML 结构示例

```html
<!DOCTYPE html>
<html>
<head>
  <style>
    /* 布局容器 */
    .layout {
      display: grid;
      grid-template-columns: 250px 1fr;
      gap: 20px;
      padding: 20px;
      resize: horizontal;
      overflow: auto;
      border: 2px dashed #ccc;
      min-width: 300px;
      max-width: 1200px;
    }

    /* 侧边栏容器查询 */
    .sidebar {
      container-type: inline-size;
      container-name: sidebar;
      background: #f8f9fa;
      border-radius: 8px;
      padding: 16px;
    }

    /* 主内容区容器查询 */
    .main-content {
      container-type: inline-size;
      container-name: main;
      background: white;
      border-radius: 8px;
      padding: 16px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.1);
    }

    /* 侧边栏适配 */
    @container sidebar (max-width: 200px) {
      .nav-item {
        flex-direction: column;
        text-align: center;
        padding: 8px;
      }
      .nav-text { font-size: 12px; }
    }

    @container sidebar (min-width: 200px) {
      .nav-item {
        flex-direction: row;
        text-align: left;
        padding: 12px;
      }
      .nav-text { font-size: 14px; }
    }

    /* 主内容区适配 */
    @container main (min-width: 500px) {
      .content-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }

    @container main (min-width: 800px) {
      .content-grid {
        grid-template-columns: repeat(3, 1fr);
      }
    }

    /* 基础样式 */
    .nav-item {
      display: flex;
      align-items: center;
      gap: 12px;
      margin-bottom: 8px;
      border-radius: 6px;
      transition: background-color 0.2s;
    }
    .nav-item:hover { background: #e9ecef; }

    .content-grid {
      display: grid;
      gap: 16px;
    }
    .content-card {
      background: #f8f9fa;
      padding: 16px;
      border-radius: 6px;
    }
  </style>
</head>
<body>
  <div class="layout">
    <aside class="sidebar">
      <div class="nav-item">
        <span>🏠</span>
        <span class="nav-text">首页</span>
      </div>
      <div class="nav-item">
        <span>📊</span>
        <span class="nav-text">数据统计</span>
      </div>
      <div class="nav-item">
        <span>⚙️</span>
        <span class="nav-text">设置</span>
      </div>
    </aside>
    
    <main class="main-content">
      <div class="content-grid">
        <div class="content-card">卡片 1</div>
        <div class="content-card">卡片 2</div>
        <div class="content-card">卡片 3</div>
        <div class="content-card">卡片 4</div>
        <div class="content-card">卡片 5</div>
        <div class="content-card">卡片 6</div>
      </div>
    </main>
  </div>
</body>
</html>
```

## ⚠️ 浏览器兼容性说明

```css
/* 渐进增强方案 */
.card {
  /* 基础样式，所有浏览器都支持 */
  display: block;
  padding: 16px;
}

/* 支持容器查询的浏览器 */
@supports (container-type: inline-size) {
  .card-container {
    container-type: inline-size;
  }
  
  @container (min-width: 400px) {
    .card {
      display: flex;
      padding: 24px;
    }
  }
}
```

## 💡 最佳实践

1. **命名容器**：使用 `container-name` 提高可读性
2. **合理断点**：基于组件功能设置断点，而不是设备尺寸
3. **渐进增强**：为不支持容器查询的浏览器提供基础样式
4. **性能考虑**：避免过度复杂的容器查询嵌套

**容器查询是现代响应式设计的未来**，特别适合组件库和设计系统开发！