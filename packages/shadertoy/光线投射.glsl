

void main(){
    vec2 play=vec2(0,0);
    float width=iResolution.x;
    float height=iResolution.y;
    vec3 col=vec3(0);
    float[2] f=float[2]{3};
    for(float x=0.;x<width;x++){

    }
    gl_FragColor=vec4(col,1);
}